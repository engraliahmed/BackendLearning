<form action="/favourites" method="POST">
    <button type="submit"
        class="rounded-xl bg-orange-800 px-4 py-2 text-sm font-semibold text-white shadow-sm transition-all duration-100 hover:bg-orange-600 hover:shadow-md ">Add
        to Favourite</button>
    <input type="hidden" name="id" value="<%= home._id %>">
</form>